import styled from 'styled-components';

export default styled.span`
  width: 100%;
  align-self: center;
  padding-top: 14px;
  padding-bottom: 14px;
  color: inherit;
  font-size: 20px;
  font-weight: 400;

  &:hover {
    color: inherit;
  }
`;